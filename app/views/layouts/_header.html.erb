<header id="header">
  <% if user_signed_in? %>
    <p>navbar</p>
  <% end %>
  <p id="header-title"><%= h2 %></p>
  <% if user_signed_in? %>
    <div class="nav-logout">
      <%= link_to("Log out", destroy_user_session_path, method: :delete, class: 'logout-link') %>
    </div>
  <%end%>
</header>
